<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Benvenuto!
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
		{{ vite_entry_link_tags('app') }}
		{% block stylesheets %}{% endblock %}
	</head>
	<body
		class="bg-dark text-white {% block body_class %}{% endblock %}">

		{# Questo div principale gestisce il layout sticky footer #}
		<div
			class="container-fluid d-flex flex-column min-vh-100 p-0">

			{# 1. L'header va DENTRO il div principale #}
			<header>
				<nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
					<div class="container-fluid">
						<a class="navbar-brand" href="{{ path('app_home') }}">Twitter_X</a>
						<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
							<span class="navbar-toggler-icon"></span>
						</button>
						<div class="collapse navbar-collapse" id="navbarNav">
							<ul
								class="navbar-nav ms-auto">
								{# Questo è l'intero blocco da incollare dentro <ul class="navbar-nav ms-auto"> #}

								{% if app.user %}
									<li class="nav-item dropdown">
										<a
											class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdownUserLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">

											{# --- LOGICA PER L'AVATAR (semplice e chiara) --- #}
											{% if app.user.userProfile and app.user.userProfile.image %}
												<img src="{{ asset('uploads/avatars/' ~ app.user.userProfile.image) }}" alt="Avatar" class="nav-avatar me-2">
											{% else %}
												<img src="https://ui-avatars.com/api/?name={{ app.user.userProfile.username|default(app.user.email)|url_encode }}&background=0D8ABC&color=fff&size=32" alt="Avatar" class="nav-avatar me-2">
											{% endif %}

											{# --- LOGICA PER IL NOME UTENTE (semplice e chiara) --- #}
											{% if app.user.userProfile and app.user.userProfile.username %}
												{{ app.user.userProfile.username }}
											{% else %}
												{{ app.user.email }}
											{% endif %}

										</a>
										<ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end" aria-labelledby="navbarDropdownUserLink">
											<li>
												<a class="dropdown-item" href="{{ path('app_profile', {'user': app.user.id}) }}"data-turbo-preload>Il Mio Profilo</a>
											</li>
											<li>
												<a class="dropdown-item" href="{{ path('app_settings_profile') }}"data-turbo-preload>Impostazioni</a>
											</li>
											<li><hr class="dropdown-divider"></li>
											<li>
												<a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
											</li>
										</ul>
									</li>
								{% else %}
									{# Questa parte per gli utenti non loggati è già corretta #}
									<li class="nav-item">
										<a class="nav-link" href="{{ path('app_login') }}">
											<i class="fas fa-sign-in-alt"></i>
											Login</a>
									</li>
									<li class="nav-item">
										<a class="nav-link" href="{{ path('app_register') }}">Registrati</a>
									</li>
								{% endif %}
							</ul>
						</div>
					</div>
				</nav>
				{% block page_header %}{% endblock %}
			</header>

			{# 2. Il main e il footer vanno DENTRO il div principale, ma DOPO l'header #}
			<main class="flex-grow-1">
				<div class="container mt-4">
					{% for label, messages in app.flashes %}
						{% for message in messages %}
							<div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
								{{ message }}
								<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
							</div>
						{% endfor %}
					{% endfor %}

					{% block body %}{% endblock %}
				</div>
			</main>

			<footer class="text-center text-muted py-3 mt-auto">
				<small>Twitter Clone &copy;
					{{ "now"|date("Y") }}</small>
			</footer>

		</div>
		{# Chiusura del div .container-fluid.d-flex #}

		{# 3. Gli script vanno qui, appena prima della singola e corretta chiusura del body #}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
		{{ vite_entry_script_tags('app') }}
		{% block javascripts %}{% endblock %}
	</body>
</html></body></html>
